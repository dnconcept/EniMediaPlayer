{% extends "EniMediaBundle::layout.html.twig" %}
{% import "EniMediaBundle:macros:buttons.html.twig" as buttons %}
{% block body %}
    <h1>Genre</h1>
    <table class="table table-bordered table-striped table-condensed">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ genre.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ genre.name }}</td>
        </tr>
        </tbody>
    </table>
    <div class='btn btn-group'>
        {{ buttons.backToList("genre", "Retour à la liste des genres") }}
        {{ buttons.editButton("genre", genre.id) }}
    </div>
    {{ buttons.deleteForm(delete_form) }}
{% endblock %}
